#include<iostream>
#include<string>
using namespace std;

class Child {
    public:
        string name;
        int age;
     
        Child(string name, int age) : name(name), age(age) {}
    };
     